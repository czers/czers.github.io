title = "czers.pl" # Homepage title and page title suffix
baseurl = "http://czers.pl/" # Controls base URL

languageCode = "en-US" # Controls html lang attribute
paginate = 5 # Number of posts to show before paginating
theme = "after-dark" # Uncomment to use as default theme

enableRobotsTXT = true # Suggested, enable robots.txt file
googleAnalytics = "" # Optional, add tracking Id for analytics
disqusShortname = "" # Optional, add Disqus shortname for comments
SectionPagesMenu = "main" # Enable menu system for lazy bloggers
footnoteReturnLinkContents = "↩" # Provides a nicer footnote return link

[params]
  description = "" # Suggested, controls default description meta
  author = "" # Optional, controls author name display on posts
  hide_author = false # Optional, set true to hide author name on posts
  show_menu = true # Optional, set true to enable section menu
  powered_by = false # Optional, set false to disable credits
  images = [] # Suggested, controls default OpenGraph images
  theme_variant = "solarized-dark" # Optional, for use to overriding default theme

[[menu.main]]
  name = "Home"
  weight = 1
  identifier = "landing-page"
  url = "/"
[[menu.main]]
  name = "Posts"
  weight = 2
  identifier = "post"
  url = "/post/"
[[menu.main]]
  name = "About"
  weight = 3
  identifier = "about"
  url = "/about/"
